<%= form_for(@projekt) do |f| %>
  <% if @projekt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projekt.errors.count, "error") %> prohibited this projekt from being saved:</h2>

      <ul>
      <% @projekt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :kommentar %><br />
    <%= f.text_field :kommentar %>
  </div>
  <div class="field">
    <%= f.label :icon %><br />
    <%= f.text_field :icon %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
